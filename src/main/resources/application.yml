spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/DAILY_STUDY_CHECK?characterEncoding=UTF-8&serverTimezone=UTC
    username: hoho_for_jpa_study
    password: 1234


  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug