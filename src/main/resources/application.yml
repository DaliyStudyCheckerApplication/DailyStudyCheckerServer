spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/DAILY_STUDY_CHECK?characterEncoding=UTF-8&serverTimezone=UTC
    username: hoho_for_jpa_study
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: false
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
logging:
  level:
    org.hibernate.SQL: debug

---
spring:
  profiles:
    active: "dev"
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://dev_database:3306/DAILY_STUDY_CHECK?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: false
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
logging:
  level:
    org.hibernate.SQL: debug